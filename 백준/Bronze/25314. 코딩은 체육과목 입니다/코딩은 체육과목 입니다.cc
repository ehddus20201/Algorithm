#include <stdio.h>


int main() {
    int a, b;
    scanf("%d", &a);
    b = a / 4;
    for (b = a / 4; b >= 1; b--)
    {
        printf("long ");
    }
    printf("int");


    return 0;
}